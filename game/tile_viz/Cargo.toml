[package]
name = "tile_viz"
version = "0.1.0"
edition = "2024"

[features]
tracing = []

[dependencies]
bevy = "0.19.0"
bevy_asset_loader = "0.27.0"
shared = { path = "../shared" }
hex = { path = "../../engine/hex" }
world_generation = { path = "../../engine/world_generation" }
